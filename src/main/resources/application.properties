spring.application.name=RealEstateHub

# spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/realestatehub?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8}
spring.datasource.url=${DB_URL:jdbc:mysql://host.docker.internal:3306/realestatehub?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:lian}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Redis
# spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.host=${REDIS_HOST:host.docker.internal}
spring.data.redis.port=${REDIS_PORT:6379}

app.jwt.secret=${JWT_SECRET:8b7c4f5e2a1d9c0f6e7b3a2c9d8f4e1b7c6a5d4e3f2b1c0a9e8d7c6b5a4f3e2}
app.jwt.expiration=${JWT_EXPIRATION:86400000}
